/*
CH-230-A
a1_p2.c
santosh luitel
sluitel@jacobs-university.de
*/
/* the function will assume you have supplied the argument and print whatever in the stack
for example if i print again without giving result it will give right answer 27
which is already in the stack. */
#include <stdio.h>
int main() {
int result; /* The result of our calculation */
result = (2 + 7) * 9 / 3;
printf("The result is %d\n",result);
 return 0;
}

